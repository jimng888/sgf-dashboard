// Generated by scripts/embed-worker-assets.js - do not edit by hand
export const login = `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Log in — SGF Team Dashboard</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body class="login-page">
  <main class="login-box">
    <h1>SGF Team Dashboard</h1>
    <p class="muted">Sign in with your team Google account to manage tickets and view system status.</p>
    <% if (typeof query !== 'undefined' && query && query.error === 'unauthorized') { %>
      <p class="error">Your account is not allowed to access this dashboard.</p>
    <% } %>
    <a href="/auth/google" class="btn btn-google">Sign in with Google</a>
  </main>
</body>
</html>
`;
export const dashboard = `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Dashboard — SGF Team</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header class="header">
    <div class="header-inner">
      <h1 class="logo">SGF Team Dashboard</h1>
      <div class="user-menu">
        <span class="user-name"><%= user.name || user.email %></span>
        <a href="/logout" class="btn btn-ghost">Log out</a>
      </div>
    </div>
  </header>
  <main class="main">
    <section class="section status-section">
      <h2>System status</h2>
      <div class="status-cards">
        <div class="status-card <%= status.live ? 'live' : 'down' %>">
          <span class="status-dot"></span>
          <div>
            <strong><%= status.live ? 'Live' : 'Down' %></strong>
            <p class="muted"><%= status.message %></p>
          </div>
        </div>
        <% if (status.sessionsCount != null) { %>
          <div class="status-card">
            <span class="status-dot neutral"></span>
            <div>
              <strong><%= status.sessionsCount %> sessions</strong>
              <p class="muted">OpenClaw</p>
            </div>
          </div>
        <% } %>
      </div>
      <p class="muted small">Status refreshes every 30s.</p>
    </section>

    <section class="section">
      <h2>Tickets</h2>
      <p class="muted">Assign tickets to <strong>Bot</strong> or <strong>you</strong> so it's clear who is in charge.</p>

      <div class="ticket-form">
        <h3>New ticket</h3>
        <form action="/api/tickets" method="post" id="new-ticket-form" class="form-inline">
          <input type="text" name="summary" placeholder="Summary (e.g. Customer wants address change)" required>
          <input type="text" name="order_number" placeholder="Order (e.g. SGF1991)">
          <input type="text" name="customer_phone" placeholder="Customer phone">
          <button type="submit" class="btn btn-primary">Create ticket</button>
        </form>
      </div>

      <div class="table-wrap">
        <table class="ticket-table">
          <thead>
            <tr>
              <th>Id</th>
              <th>Summary</th>
              <th>Order / Customer</th>
              <th>Status</th>
              <th>Assigned to</th>
              <th>Updated</th>
            </tr>
          </thead>
          <tbody>
            <% tickets.forEach(function(t) {
              const assignedLabel = t.assigned_to_type === 'bot' ? 'Bot' : (t.assigned_to_name || t.assigned_to_email || 'Unknown');
            %>
            <tr data-ticket-id="<%= t.id %>">
              <td><%= t.id %></td>
              <td><%= t.summary %></td>
              <td><%= t.order_number || '—' %> / <%= t.customer_phone || '—' %></td>
              <td><span class="badge badge-<%= t.status %>"><%= t.status %></span></td>
              <td>
                <select class="assign-select" data-ticket-id="<%= t.id %>">
                  <option value="bot" <%= t.assigned_to_type === 'bot' ? 'selected' : '' %>>Bot</option>
                  <option value="human:<%= user.id %>" <%= t.assigned_to_type === 'human' && t.assigned_to_user_id == user.id ? 'selected' : '' %>>Me</option>
                </select>
              </td>
              <td><%= t.updated_at ? new Date(t.updated_at).toLocaleString() : '—' %></td>
            </tr>
            <% }); %>
            <% if (!tickets.length) { %>
              <tr><td colspan="6" class="muted">No tickets yet. Create one above.</td></tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </section>
  </main>
  <script src="/app.js"></script>
</body>
</html>
`;
